Se tiene un servidor de archivos (File Server), donde se guardan diferentes tipos de archivos (PDF, Word, Excel).
El servidor solamente tiene la capacidad de guardar archivos PDF. Si el archivo es otra extension se
muestra un mensaje pidiendo la extensión correcta.

RealSubject -> FileServer
Proxy -> FileServerProxy
ISubject -> IServer
ConcreteObject -> File